# 链接生成页面访问密码（必需）
GENERATE_PAGE_PASSWORD=your-strong-password-here

# AES 加密密钥（必需，建议 32 字符以上）
ENCRYPTION_KEY=your-32-character-encryption-key-here

# JWT 签名密钥（必需，建议 32 字符以上）
JWT_SECRET=your-32-character-jwt-secret-key-here

# XOR 混淆密钥（可选）
OBFUSCATION_KEY=your-obfuscation-key-here

# JWT 会话超时时间，秒（可选，默认 3600）
SESSION_TIMEOUT=3600

# 默认跳转延迟时间，毫秒（可选，默认 3000）
DEFAULT_DELAY=3000

# 是否启用 Referer 检查（可选，默认 true）
ENABLE_REFERER_CHECK=true

# 是否启用延迟跳转（可选，默认 true）
ENABLE_DELAY=true

# 允许的目标域名白名单，逗号分隔（可选，留空表示允许所有）
ALLOWED_DOMAINS=example.com,example.org

# 允许的来源域名白名单，逗号分隔（可选，留空表示不检查）
ALLOWED_REFERERS=yourdomain.com,app.yourdomain.com

# 不检查 Referer 的目标域名，逗号分隔（可选）
NO_REFERER_CHECK_DOMAINS=public-site.com

# 允许空 Referer 的目标域名，逗号分隔（可选）
ALLOW_EMPTY_REFERER_DOMAINS=direct-access.com

# 统计数据 Webhook URL（可选）
WEBHOOK_URL=https://your-webhook.com/api/stats

# 注意：
# 1. 复制此文件为 .dev.vars 用于本地开发
# 2. 在 Cloudflare Pages Dashboard 中配置生产环境变量
# 3. 不要将包含真实密钥的文件提交到 Git
